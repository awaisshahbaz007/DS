import { OpenDiagDirective } from './open-diag.directive';

describe('OpenDiagDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDiagDirective();
    expect(directive).toBeTruthy();
  });
});
